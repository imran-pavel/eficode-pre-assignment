import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  trips: {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center'
  },
  tableContainer: {
    margin: 20,
    width: '90%'
  }
});